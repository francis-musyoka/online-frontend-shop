export const selectUserProfile = (state) => state.customerAuth.user;
export const selectIsAuthenticated = (state) => state.customerAuth.isAuthenticated;
export const selectError = (state) => state.customerAuth.isAuthenticated;